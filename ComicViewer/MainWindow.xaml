<Window x:Class="ComicViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComicViewer"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="ComicViewer" Height="1100" Width="800" MinHeight="420" MinWidth="360" WindowStartupLocation="CenterScreen"
        Background="#111111"
        ResizeMode="CanResize"
        WindowStyle="None"
        PreviewKeyDown="MainWindow_PreviewKeyDown"
        StateChanged="MainWindow_StateChanged">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44"
                            CornerRadius="0"
                            GlassFrameThickness="1"
                            ResizeBorderThickness="6" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="ToolbarButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#303030" />
            <Setter Property="Foreground" Value="#F2F2F2" />
            <Setter Property="BorderBrush" Value="#555555" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Height" Value="28" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#3C3C3C" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#6A6A6A" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#252525" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#777777" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CaptionButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="44" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="#F2F2F2" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Cursor" Value="Arrow" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#3A3A3A" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#4A4A4A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseCaptionButtonStyle"
               TargetType="{x:Type Button}"
               BasedOn="{StaticResource CaptionButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#C42B1C" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#A4261D" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PlayOverlayButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="100" />
            <Setter Property="Height" Value="100" />
            <Setter Property="Background" Value="#99000000" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#88FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="Segoe UI Emoji" />
            <Setter Property="FontSize" Value="36" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Visibility" Value="Visible" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="50"
                                SnapsToDevicePixels="True">
                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Padding="5,-5,0,0"
                                       FontFamily="{TemplateBinding FontFamily}"
                                       FontSize="48"
                                       Foreground="{TemplateBinding Foreground}"
                                       Text="▷" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#B0000000" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#CCFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#CC000000" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsVideoPlaying}" Value="True" />
                        <Condition Binding="{Binding IsVideoPaused}" Value="False" />
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Visibility" Value="Collapsed" />
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InvisibleScrollBarButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="{TemplateBinding Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComicScrollBarThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="Background" Value="#99000000" />
            <Setter Property="BorderBrush" Value="#88FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="ThumbBorder"
                                Margin="2"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                SnapsToDevicePixels="True" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#B0000000" />
                                <Setter TargetName="ThumbBorder" Property="BorderBrush" Value="#CCFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#CC000000" />
                                <Setter TargetName="ThumbBorder" Property="BorderBrush" Value="#E6FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComicScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="14" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Width="{TemplateBinding Width}"
                              Background="Transparent">
                            <local:MinimumThumbTrack x:Name="PART_Track"
                                                     IsDirectionReversed="True"
                                                     MinimumThumbLength="40">
                                <local:MinimumThumbTrack.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}"
                                                  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                                  Style="{StaticResource InvisibleScrollBarButtonStyle}" />
                                </local:MinimumThumbTrack.DecreaseRepeatButton>
                                <local:MinimumThumbTrack.Thumb>
                                    <Thumb Style="{StaticResource ComicScrollBarThumbStyle}" />
                                </local:MinimumThumbTrack.Thumb>
                                <local:MinimumThumbTrack.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}"
                                                  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                                  Style="{StaticResource InvisibleScrollBarButtonStyle}" />
                                </local:MinimumThumbTrack.IncreaseRepeatButton>
                            </local:MinimumThumbTrack>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComicScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid Background="{TemplateBinding Background}"
                              ClipToBounds="True">
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                    Margin="{TemplateBinding Padding}" />
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Panel.ZIndex="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Stretch"
                                       Margin="0,4,2,4"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       Orientation="Vertical"
                                       Style="{StaticResource ComicScrollBarStyle}"
                                       Value="{Binding VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Visibility="Collapsed" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComicPageListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComicPagesListBoxStyle" TargetType="{x:Type ListBox}">
            <Setter Property="Background" Value="#111111" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel" />
            <Setter Property="VirtualizingPanel.CacheLength" Value="2" />
            <Setter Property="VirtualizingPanel.CacheLengthUnit" Value="Page" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource ComicPageListBoxItemStyle}" />
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <ScrollViewer x:Name="PART_ScrollViewer"
                                      Background="{TemplateBinding Background}"
                                      CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}"
                                      HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                      Padding="{TemplateBinding Padding}"
                                      Style="{StaticResource ComicScrollViewerStyle}"
                                      VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Background="#202020" LastChildFill="True">
            <Button x:Name="OpenArchiveButton"
                    DockPanel.Dock="Left"
                    MinWidth="96"
                    Margin="10,8"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Style="{StaticResource ToolbarButtonStyle}"
                    Click="OpenArchiveButton_Click"
                    Content="打开压缩包" />
            <Button x:Name="CloseWindowButton"
                    DockPanel.Dock="Right"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Style="{StaticResource CloseCaptionButtonStyle}"
                    Click="CloseWindowButton_Click"
                    Content="&#xE8BB;" />
            <Button x:Name="MaximizeRestoreWindowButton"
                    DockPanel.Dock="Right"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Style="{StaticResource CaptionButtonStyle}"
                    Click="MaximizeRestoreWindowButton_Click"
                    Content="&#xE922;" />
            <Button x:Name="MinimizeWindowButton"
                    DockPanel.Dock="Right"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Style="{StaticResource CaptionButtonStyle}"
                    Click="MinimizeWindowButton_Click"
                    Content="&#xE921;" />
            <TextBlock x:Name="StatusTextBlock"
                       VerticalAlignment="Center"
                       Foreground="#D8D8D8"
                       TextTrimming="CharacterEllipsis"
                       Text="请选择 zip 或 rar 漫画压缩包" />
        </DockPanel>

        <ListBox x:Name="PagesListBox"
                 Grid.Row="1"
                 ItemsSource="{Binding Pages}"
                 PreviewMouseWheel="ImageScrollViewer_PreviewMouseWheel"
                 ScrollViewer.ScrollChanged="ImageScrollViewer_ScrollChanged"
                 SizeChanged="ImageScrollViewer_SizeChanged"
                 Style="{StaticResource ComicPagesListBoxStyle}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Width="{Binding DisplayWidth}"
                          Height="{Binding DisplayHeight}"
                          HorizontalAlignment="Left"
                          Background="#050505"
                          SnapsToDevicePixels="True"
                          UseLayoutRounding="True">
                        <Image Source="{Binding Image}"
                               Stretch="Fill">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsImage}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>

                        <Grid Background="Transparent"
                              MouseLeftButtonDown="VideoSurface_MouseLeftButtonDown">
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsVideo}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Image Source="{Binding VideoFrame}"
                                   Stretch="Uniform"
                                   IsHitTestVisible="False">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding VideoFrame}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>

                            <Button HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource PlayOverlayButtonStyle}"
                                    Click="PlayVideoButton_Click" />
                        </Grid>

                        <StackPanel HorizontalAlignment="Left"
                                    VerticalAlignment="Bottom"
                                    Margin="8"
                                    Orientation="Horizontal"
                                    IsHitTestVisible="False">
                            <Border Padding="8,3"
                                    Background="#99000000"
                                    CornerRadius="3">
                                <TextBlock Foreground="White"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Text="{Binding DisplayNumber}" />
                            </Border>
                            <Border Margin="6,0,0,0"
                                    Padding="8,3"
                                    Background="#99000000"
                                    CornerRadius="3">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsVideo}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Foreground="White"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Text="{Binding VideoTimeText}" />
                            </Border>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid x:Name="PasswordOverlay"
              Grid.Row="1"
              Background="#CC000000"
              Visibility="Collapsed">
            <Border Width="420"
                    MaxWidth="520"
                    Padding="22"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="#202020"
                    BorderBrush="#555555"
                    BorderThickness="1"
                    CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="输入压缩包密码"
                               Foreground="#F2F2F2"
                               FontSize="18"
                               FontWeight="SemiBold" />

                    <TextBlock x:Name="PasswordArchiveNameTextBlock"
                               Grid.Row="1"
                               Margin="0,8,0,0"
                               Foreground="#BDBDBD"
                               TextTrimming="CharacterEllipsis" />

                    <PasswordBox x:Name="ArchivePasswordBox"
                                 Grid.Row="2"
                                 Height="34"
                                 Margin="0,16,0,0"
                                 Padding="8,0"
                                 VerticalContentAlignment="Center"
                                 Background="#111111"
                                 Foreground="#F2F2F2"
                                 BorderBrush="#666666"
                                 KeyDown="ArchivePasswordBox_KeyDown"
                                 PasswordChanged="ArchivePasswordBox_PasswordChanged"/>

                    <Grid Grid.Row="3"
                          Margin="0,16,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="历史密码（双击使用）"
                                   Foreground="#BDBDBD"
                                   FontSize="13" />

                        <ListBox x:Name="PasswordHistoryListBox"
                                 Grid.Row="1"
                                 Height="118"
                                 Margin="0,8,0,0"
                                 ItemsSource="{Binding PasswordHistory}"
                                 Background="#151515"
                                 Foreground="#F2F2F2"
                                 BorderBrush="#444444"
                                 MouseDoubleClick="PasswordHistoryListBox_MouseDoubleClick" />
                    </Grid>

                    <StackPanel Grid.Row="4"
                                Margin="0,18,0,0"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button MinWidth="82"
                                Margin="0,0,8,0"
                                Style="{StaticResource ToolbarButtonStyle}"
                                Click="PasswordCancelButton_Click"
                                Content="取消" />
                        <Button x:Name="PasswordOpenButton"
                                MinWidth="82"
                                Style="{StaticResource ToolbarButtonStyle}"
                                Click="PasswordOpenButton_Click"
                                IsEnabled="False"
                                Content="打开" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
